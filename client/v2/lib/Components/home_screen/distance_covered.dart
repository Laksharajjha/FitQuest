import 'package:flutter/material.dart';

class DistanceCoveredScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Distance Covered'),
      ),
      body: Center(
        child: Text('Details about Steps Counter'),
      ),
    );
  }
}


