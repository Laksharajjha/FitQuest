import 'package:flutter/material.dart';

class SleepDurationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sleep Duration'),
      ),
      body: Center(
        child: Text('Details about Steps Counter'),
      ),
    );
  }
}


